import xlrd

# File to read
excel_file = "dynamic_excel_old.xls"

# Open the workbook
workbook = xlrd.open_workbook(excel_file)
worksheet = workbook.sheet_by_index(0)

# Print all rows
for row_index in range(worksheet.nrows):
    print(worksheet.row_values(row_index))
